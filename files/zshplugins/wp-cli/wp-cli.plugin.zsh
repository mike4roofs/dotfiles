# Aliases

# wp core [download|config|install|version|update]
alias wpc='wp core'
alias wpcd='wp core download'
alias wpcc='wp core config'
alias wpcu='wp core update'
alias wpcv='wp core version --extra'
alias wpci='wp core install'

# wp db [connect|cli|export|query|import]
alias wpdco="wp db connect"
alias wpdcl="wp db cli"
alias wpde="wp db export"
alias wpdq="wp db query"
alias wpdi="wp db import"

# wp eval-file
alias wpef="wp eval-file"

# wp eval
alias wpev="wp eval"

# wp export [validate_arguments]
alias wpe="wp export"

# wp generate [posts|users]
alias wpgp="wp generate posts"
alias wpgu="wp generate users"

# wp home
alias wph="wp home"

# wp option [add|update|delete|get]
alias wpoa="wp option add"
alias wpou="wp option update"
alias wpod="wp option delete"
alias wpog="wp option get"

# wp plugin [status|activate|deactivate|toggle|path|install|uninstall|delete|update]
alias wpp="wp plugin"
alias wpps="wp plugin status"
alias wppa="wp plugin activate"
alias wppd="wp plugin deactivate"
alias wppt="wp plugin toggle"
alias wppp="wp plugin path"
alias wppi="wp plugin install"
alias wppun="wp plugin uninstall"
alias wppdl="wp plugin delete"
alias wppup="wp plugin u"

# wp theme [status|activate|path|delete|update]
alias wpt="wp theme"
alias wpts="wp theme status"
alias wpta="wp theme activate"
alias wptp="wp theme path"
alias wptd="wp theme delete"
alias wptu="wp theme update"

# wp user [list|delete|create|update]
alias wpu="wp user"
alias wpul="wp user list"
alias wpud="wp user delete"
alias wpuc="wp user create"
alias wpuu="wp user update"
